server {
    # Nginx가 수신 대기할 포트 번호
    listen 80;
    server_name localhost;

    # 기본 문서 루트 및 인덱스 파일 설정
    root /usr/share/nginx/html;
    index index.html;

    # 모든 요청을 index.html로 리디렉션하여 SPA 라우팅 처리
    location / {
        try_files $uri /index.html;
    }
}
